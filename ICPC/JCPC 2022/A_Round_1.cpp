#include <bits/stdc++.h>
#define ll long long
#define endl '\n'

using namespace std;

void testCase() {
    int N;
    cin >> N;
    cout << (N <= 26 ? "Yes" : "No") << endl;
}

int main() {
    freopen("Round1.in", "r", stdin);

    int t;
    cin >> t;

    while (t--) {
        testCase();
    }

    cout << flush;
    return 0;
}