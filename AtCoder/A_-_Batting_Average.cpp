#include <bits/stdc++.h>
#define ll long long
#define endl '\n'

using namespace std;

int main() {
    double a, b;
    cin >> a >> b;
    cout << fixed << setprecision(3) << b / a << endl;
    cout << flush;
    return 0;
}